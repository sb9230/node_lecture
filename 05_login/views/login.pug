extends layout

block content
  form(method="POST" action="/login")
    #error
      if error
        p 오류가 발생했습니다. 다시 시도하세요.
    p
      label(for="input-id") 아이디
      input#input-id(type="text" name="id")
    p
      label(for="input-password") 비밀번호
      input#input-password(type="password" name="password")
    button(type="submit") 로그인